---
phase: 02-azul-first-playable
task: puppeteer-verification
total_tasks: 10
status: in_progress
last_updated: 2026-03-01T23:22:56.459Z
---

<current_state>
Running Phase 2 UAT via puppeteer (test-game.mjs). The interactive test now PASSES — 4/4 moves succeed. Two bugs were found and fixed during verification. UAT file created at 02-UAT.md but not yet walked through (user wants puppeteer-driven verification first, then human verification).

All changes are UNCOMMITTED on `master` branch.
</current_state>

<completed_work>

- Dev logging: Added structured `[gameApi]` logging to all 3 API functions in gameApi.ts
- Dev logging: Added structured `[SceneManager]` logging at init, handleSourceClick, handleDestinationClick, submitPlayerMove, handleMoveResult
- Dev logging: Added light `[AzulScene]` logging at renderBoard, selectSource, destination callbacks
- Window exposure: `window.__sm = sceneManager` in +page.svelte for puppeteer access
- Puppeteer test: Created test-game.mjs with request interception (localhost:8080 → server:8080), move execution, screenshots
- Dockerfile: Updated to copy both scripts, CMD instead of ENTRYPOINT
- **Bug fix 1**: TS stripper in HookExecutor.cs step 12b was matching `defId: FIRST_PLAYER_TOKEN` as type annotation (SCREAMING_SNAKE starts with uppercase). Fixed with negative lookahead `(?![A-Z_]*\s*[,)=])` to exclude all-caps constants
- **Bug fix 2**: AzulScene COLOR_MAP/LABEL_MAP missing 'white' color — server uses 'white' as 5th Azul tile color but client only had 'teal'. Added white: '#D4E4F7' and label 'W'
- Puppeteer test now passes: 4/4 moves, alternating players, center picks with first-player-token work correctly
</completed_work>

<remaining_work>

- Non-blocking warning: `Unable to convert color -7306` during PixiJS re-render after moves 3-4 (doesn't affect game logic, but scene re-render partially fails)
- Non-blocking warning: `Event "SUBMIT_MOVE" was sent to stopped actor` — FSM actor reaches final state, moves 3-4 trigger warning
- UAT human verification: 02-UAT.md has 10 tests — user should walk through with running app after puppeteer confirms basic flow works
- Commit all changes (logging, puppeteer, bug fixes)
- Typecheck passes (confirmed before bug fixes, should re-verify after HookExecutor.cs change)
</remaining_work>

<decisions_made>

- TS stripper fix uses negative lookahead `(?![A-Z_]*\s*[,)=])` rather than requiring `[A-Z][a-z]` (PascalCase) — the latter was too strict and broke single-letter type params
- White tile color rendered as '#D4E4F7' (light blue-gray) rather than '#FFFFFF' to be visible on the light background
- Puppeteer request interception rewrites localhost:8080 to server:8080 for Docker network compatibility
</decisions_made>

<blockers>
- `Unable to convert color -7306` in PixiJS — happens during setActivePlayer re-render. Likely a PixiJS color value being passed as a negative int instead of hex. Non-blocking but should be investigated.
</blockers>

<context>
The goal was to build a puppeteer verification harness and use it to smoke-test Phase 2 before human UAT. The harness exposed two real bugs: (1) TS stripper mangling object literals with SCREAMING_SNAKE constants, (2) missing 'white' color mapping. Both fixed, test passes 4/4 moves. The structured logging is working well — console output clearly shows the full request→click→submit→result flow.

Files modified (uncommitted):
- apps/client/src/routes/game/[id]/+page.svelte (window.__sm)
- apps/client/src/lib/api/gameApi.ts (logging)
- apps/client/src/lib/engine/SceneManager.ts (logging)
- apps/client/src/lib/engine/AzulScene.ts (logging + white color)
- apps/server/Services/HookExecutor.cs (TS stripper fix)
- apps/infra/screenshot/test-game.mjs (new)
- apps/infra/screenshot/Dockerfile (updated)
</context>

<next_action>
Start with: Commit all changes, then ask user to do human UAT walkthrough with the running app (http://localhost:5173/). The PixiJS color -7306 warning can be investigated as a follow-up.
</next_action>
