---
phase: 01-engine-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nx.json
  - package.json
  - tsconfig.base.json
  - apps/client/svelte.config.js
  - apps/client/vite.config.ts
  - apps/client/tsconfig.json
  - apps/client/package.json
  - apps/client/src/app.html
  - apps/client/src/routes/+page.svelte
  - apps/server/Program.cs
  - apps/server/server.csproj
  - apps/infra/docker-compose.yml
  - apps/infra/project.json
  - apps/client/Dockerfile
  - apps/server/Dockerfile
  - libs/engine/core/package.json
  - libs/engine/core/tsconfig.json
  - libs/engine/core/src/index.ts
  - libs/shared-types/package.json
  - libs/shared-types/tsconfig.json
  - libs/shared-types/src/index.ts
  - libs/games/azul/package.json
  - libs/games/azul/tsconfig.json
  - .gitignore
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "docker compose up starts all 4 services (client, server, db, localstack) without errors"
    - "SvelteKit dev server responds on http://localhost:5173 with a page"
    - "C# API server responds on http://localhost:8080"
    - "PostgreSQL accepts connections on localhost:5432"
    - "NX workspace graph recognizes all projects (apps/client, apps/server, libs/engine-core, libs/shared-types, libs/games-azul)"
    - "TypeScript path aliases (@bga2/*) resolve correctly in the SvelteKit app"
  artifacts:
    - path: "apps/infra/docker-compose.yml"
      provides: "Docker Compose orchestration for 4 services"
      contains: "services:"
    - path: "nx.json"
      provides: "NX workspace configuration"
      contains: "targetDefaults"
    - path: "tsconfig.base.json"
      provides: "TypeScript path aliases for monorepo"
      contains: "@bga2/"
    - path: "apps/client/vite.config.ts"
      provides: "SvelteKit Vite config with NX path resolution"
      contains: "nxViteTsPaths"
  key_links:
    - from: "apps/client/vite.config.ts"
      to: "tsconfig.base.json"
      via: "nxViteTsPaths() resolves @bga2/* aliases"
      pattern: "nxViteTsPaths"
    - from: "apps/infra/docker-compose.yml"
      to: "apps/client/Dockerfile"
      via: "Docker build context"
      pattern: "build:"
---

<objective>
Scaffold the NX monorepo workspace with all project directories, configure SvelteKit and C# apps, set up Docker Compose with 4 services, and verify the full dev stack boots.

Purpose: This is the foundation everything else builds on. No code can be written until the project structure, build system, and dev environment exist.
Output: A bootable NX monorepo with Docker Compose that starts all 4 services.
</objective>

<execution_context>
@/home/emkayultra/.claude/get-shit-done/workflows/execute-plan.md
@/home/emkayultra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-foundation/01-CONTEXT.md
@.planning/phases/01-engine-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold NX monorepo with all project structure</name>
  <files>
    nx.json, package.json, tsconfig.base.json,
    apps/client/svelte.config.js, apps/client/vite.config.ts, apps/client/tsconfig.json, apps/client/package.json, apps/client/src/app.html, apps/client/src/routes/+page.svelte,
    apps/server/Program.cs, apps/server/server.csproj,
    apps/infra/project.json,
    libs/engine/core/package.json, libs/engine/core/tsconfig.json, libs/engine/core/src/index.ts,
    libs/shared-types/package.json, libs/shared-types/tsconfig.json, libs/shared-types/src/index.ts,
    libs/games/azul/package.json, libs/games/azul/tsconfig.json,
    .gitignore
  </files>
  <action>
    1. Initialize NX workspace: `npx create-nx-workspace@latest bga2 --preset=apps --packageManager=npm --nxCloud=skip` (or manually create nx.json + package.json if the workspace already exists as a git repo). Since this IS already a git repo, manually create the NX workspace files in-place.

    2. Create `nx.json` with:
       - `targetDefaults` for build, test, lint
       - `defaultBase: "main"`
       - `namedInputs` for production and default

    3. Create `tsconfig.base.json` at workspace root with path aliases:
       - `@bga2/engine-core` -> `libs/engine/core/src/index.ts`
       - `@bga2/shared-types` -> `libs/shared-types/src/index.ts`
       - `@bga2/games-azul` -> `libs/games/azul/src/index.ts`
       - Target: ES2022, module: ESNext, moduleResolution: bundler

    4. Create SvelteKit app in `apps/client/`:
       - `svelte.config.js` with adapter-auto
       - `vite.config.ts` with `sveltekit()` and `nxViteTsPaths()` plugins. Add `server.host: '0.0.0.0'` and `server.hmr.port: 24678` for Docker.
       - `tsconfig.json` extending `../../tsconfig.base.json`
       - `package.json` with svelte, sveltekit, vite dependencies
       - `src/app.html` basic HTML shell
       - `src/routes/+page.svelte` minimal landing page
       - Install SvelteKit dependencies: `npm install @sveltejs/kit @sveltejs/adapter-auto svelte vite @sveltejs/vite-plugin-svelte @nx/vite`

    5. Create C# API server in `apps/server/`:
       - `server.csproj` targeting net8.0 with Npgsql.EntityFrameworkCore.PostgreSQL and Jint NuGet packages
       - `Program.cs` with minimal API setup, a health check endpoint at GET /, CORS allowing localhost:5173

    6. Create `apps/infra/project.json` as an NX project for infrastructure scripts.

    7. Create library stubs:
       - `libs/engine/core/` with package.json, tsconfig.json, src/index.ts (empty barrel export)
       - `libs/shared-types/` with package.json, tsconfig.json, src/index.ts (empty barrel export)
       - `libs/games/azul/` with package.json, tsconfig.json

    8. Create/update `.gitignore` with node_modules, dist, .nx, bin, obj, .vs patterns.

    9. Run `npm install` to install all dependencies.

    10. Verify NX recognizes all projects: `npx nx show projects` should list client, server, engine-core, shared-types, games-azul.

    NOTE: Do NOT use deprecated `@nxext/sveltekit` or `@nx-dotnet/core`. Use `@nx/vite` with manual SvelteKit config. Use `@nx/dotnet` if available, otherwise configure C# as a custom NX project with shell executors.
  </action>
  <verify>
    <automated>npx nx show projects 2>/dev/null | sort</automated>
  </verify>
  <done>NX workspace exists with all 5+ projects recognized. TypeScript compiles without errors. npm install succeeded.</done>
</task>

<task type="auto">
  <name>Task 2: Create Docker Compose with 4 services and Dockerfiles</name>
  <files>
    apps/infra/docker-compose.yml,
    apps/client/Dockerfile,
    apps/server/Dockerfile
  </files>
  <action>
    1. Create `apps/client/Dockerfile` (multi-stage):
       - Stage 1 (base): node:20-alpine, WORKDIR /app, copy package*.json and install deps
       - Stage 2 (dev): copy source, expose 5173 + 24678, CMD npm run dev -- --host 0.0.0.0
       - Target the dev stage for docker-compose

    2. Create `apps/server/Dockerfile` (multi-stage):
       - Stage 1 (base): mcr.microsoft.com/dotnet/sdk:8.0, WORKDIR /app
       - Stage 2 (dev): copy source, expose 8080, set DOTNET_USE_POLLING_FILE_WATCHER=1, CMD dotnet watch run
       - Target the dev stage for docker-compose

    3. Create `apps/infra/docker-compose.yml` with 4 services per CONTEXT.md decisions:
       - `client`: builds from apps/client/Dockerfile, ports 5173:5173 + 24678:24678, volume-mounts apps/client and libs for HMR, sets CHOKIDAR_USEPOLLING=true
       - `server`: builds from apps/server/Dockerfile, port 8080:8080, volume-mounts apps/server, sets DOTNET_USE_POLLING_FILE_WATCHER=1 and ConnectionStrings__Default pointing to db service
       - `db`: postgres:16-alpine image, POSTGRES_DB=bga2, POSTGRES_USER=bga2, POSTGRES_PASSWORD=secret, port 5432:5432, named volume pgdata
       - `localstack`: localstack/localstack:latest, port 4566:4566, SERVICES=s3,ses, named volume localstack_data
       - Declare volumes: pgdata, localstack_data
       - Add healthchecks for db service (pg_isready)

    4. Verify: `docker compose -f apps/infra/docker-compose.yml config` should validate without errors.

    NOTE: Use build context of `../..` (workspace root) so Dockerfiles can COPY from any workspace path. Set CHOKIDAR_USEPOLLING=true for client and DOTNET_USE_POLLING_FILE_WATCHER=1 for server per research pitfalls.
  </action>
  <verify>
    <automated>docker compose -f apps/infra/docker-compose.yml config --quiet 2>&1 && echo "VALID"</automated>
  </verify>
  <done>docker-compose.yml validates. Dockerfiles exist for client and server. All 4 services defined with correct ports, volumes, and environment variables.</done>
</task>

</tasks>

<verification>
- `npx nx show projects` lists all expected projects
- `docker compose -f apps/infra/docker-compose.yml config` validates without errors
- `npx tsc --noEmit -p tsconfig.base.json` compiles without errors (or individual project tsc checks)
- SvelteKit vite.config.ts includes nxViteTsPaths()
</verification>

<success_criteria>
- NX monorepo scaffold complete with apps/client, apps/server, apps/infra, libs/engine/core, libs/shared-types, libs/games/azul
- Docker Compose file with 4 services (client, server, db, localstack) validates
- TypeScript path aliases (@bga2/*) configured in tsconfig.base.json
- SvelteKit configured with nxViteTsPaths() for NX path resolution
- C# project file with required NuGet dependencies (Npgsql, Jint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-foundation/01-01-SUMMARY.md`
</output>
